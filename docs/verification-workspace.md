# 文件工作区与可重放引擎候选验收

记录日期：2026-09-18（Asia/Shanghai）。`0.1.0-dev.6` 已完成本地编译制品门禁，未安装到现用身份，未发布为 GitHub Release。当前实际安装仍为 `.5` + engine.5。

## 候选与来源

| 对象 | 实际值 |
| --- | --- |
| 产品候选 | `dist/xingyao-0.1.0-dev.6`，Windows x64 / schema 2 |
| 引擎版本 | `0.0.0-product-dev-20260918-engine.6-source` |
| release.json SHA-256 | `83f4881c50014297be10909b9f897a4b8473d13a5f1a5d2826f1c747b0158696` |
| xingyao.exe SHA-256 | `2274955bcaa403938de035c2b1b486c8e3de1f35d8b96c9e58f603f178aa37f2` |
| opencode.exe SHA-256 | `f02b6bbba598d4f2e9e1c1f75794104b9581129ae199d3130b4bc2e552ee748b` |
| 产品 src tree SHA-256 | `9a506214843e7ec598420c311ddc940c05856e066e51b69005005b5194dc1e28` |
| 公开上游 commit | `e03db9bc6908f75c9334d8aa997deeaac81c0298` |
| 四份补丁后的引擎 tree | `1a4d9ee6b75e5f7de5938c5e3483f970fbab55fe` |

完整源码重放、模型快照、工具版本和历史限制见 [引擎来源](engine-source.md)。候选包包含全部补丁、manifest、许可证及与 exe/许可证/源码清单匹配的 `engine-build.json`。两个干净构建的二进制哈希不同，未声称可逐字节复现。

## 实际验收

`bun run script/verify-release.ts` 完成类型检查、**304 项测试 / 0 fail / 0 skip / 9,257 个断言 / 29 个文件**，测试用时 79.14 秒；随后实际编译程序与 Edge `153.0.4234.32` 的浏览器验收通过。完整日志为 `reports/2026-09-17T17-45-05-685Z-tests.txt`，候选内 `release-validation.json` 绑定上表 release manifest。

测试包含真实 OpenCode 与本地模型协议夹具、shell 成功/失败/超时/拒绝、普通与 Git 项目跨物理路径恢复、schema 迁移、编译程序跨宿主恢复，以及全部既有领域回归。未使用公网模型凭据。

文件工作区回归覆盖读取版本与身份、外部修改冲突、父目录替换、实际 junction/硬链接、真实跨进程保存锁、八个捕获失败边界、外部写入保全，以及真实进程强退后的备份保全和遗留锁回收。知识导入增加并发回归：旧刷新不能覆盖编辑器更新的内容、范围和私密标记。

浏览器验收实际点击目录和文件，验证未保存导航取消、Ctrl+S、BOM/CRLF、原件备份、私密范围下的索引刷新、约 300 KB / 1,000 行文本保存、外部修改冲突和重新读取。390 像素与 1,600 像素宽度均检查。原有图谱来源、分页、筛选、纠正、XSS 文本渲染、陈旧版本提示继续通过；0 页面错误、0 CSP 错误、0 外部请求。报告为 `reports/graph-browser-0.1.0-dev.6.json`，绑定最终 exe 哈希。

## 尚未达到的条件

- `.5` 的 engine.5 检查点不能直接由 engine.6 恢复；现有恢复器要求版本精确匹配。此次同版本搬迁测试不证明跨版本迁移，因此未切换当前安装。
- 文件编辑的草稿与授权根只在当前页面/进程有效。崩溃或断电后的编辑日志自动重放尚未实现；中断期间可能缺失或部分写入目标，原件/前像/拟写稿留在 `.xingyao-edit-*`。普通错误回滚依赖硬链接；不支持时保留恢复副本并报告待核实。
- 本机原生 textarea 一次输入 25,000 行触发 30 秒超时。减少到约 300 KB / 1,000 行的实际输入与保存通过；大量短行输入性能仍需优化。未以缩短测试内容宣称 25,000 行场景通过。
- 编辑备份、检查点和附件尚未完成长期容量管理；中文模型记忆质量、子智能体 UI、Infinite Canvas 集成与 90 天运行测量仍是后续工作。
- `.github/workflows/windows-candidate.yml` 只完成本地编写和 YAML 检查，没有 GitHub hosted runner 的成功记录。公开仓库当前仍以聊天档案分支为默认入口。
