# V2 实施与验收状态

本记录区分已经实现的能力与完整架构中的后续工作，不将设计稿当作已实现功能。

2026-09-17 架构复查确认了工具结果误判、承诺遗漏、重复修订计数和旧记忆新鲜度重置等问题，历史基线见 [架构反省与修正优先级](architecture-review-2026-09-17.md)。`0.1.0-dev.5` 源码已修复上述问题，并新增 schema 1→2 迁移及恢复分支保护；实际 shell、HTTP、领域和启动迁移定向回归已通过，详见 [证据修复说明](evidence-fixes-2026-09-17.md)。发行是否通过完整验收、安装了哪个制品，以对应发行验证记录为准。

## 已实现，验收结果绑定具体发行制品

- 独立 Bun/SQLite 领域运行时；稳定身份；根来源幂等；情绪/心境闭式传播。
- 事实、偏好、推论、承诺、情节的范围与来源；纠正、封存、删除及同源删除标记。
- 记忆检索按词项相关度和新鲜度评分；新鲜度采用 14 天半衰期，固定记忆保留。该衰减不等于自动删除源记录。
- 前台任务、唯一 OpenCode 执行路径、权限响应、实际工具结果对账、unknown 恢复。
- 手动与运行期间空闲睡眠；处理游标与归档同事务，封存策略遵循事件进入时状态。
- 中文资料检索、引用位置、增量更新、文件整理后的稳定资料引用。
- `0.1.0-dev.3` 新增本地 Cytoscape 知识图谱、已导入资料目录树、名称与类型筛选、已加载邻居浏览、来源正文分页和记忆/经历详情。范围和私密标记先在查询中过滤，文档版本变化后要求刷新图谱；具体发行验收记录另行绑定制品。
- `0.1.0-dev.4` 调整图谱布局的初始位置、节点间距与标签尺寸参与方式，减少切换筛选后的节点重叠；窗口缩放后自动适配视图。
- `0.1.0-dev.5` 保留工具结构化结果和退出码，失败/未知不再记作成功；同一调用修订只保留一份有效贡献；按来源发生时间计算记忆年龄；固定约束超预算时拒绝不完整投影。旧数据缺少执行证据时降为未知并使旧派生结论失效，保留历史原文。
- `0.1.0-dev.6` 本地候选增加选择目录、文件树、UTF-8 文本编辑、修改冲突检测、原件备份和保存后知识索引刷新；完整编译制品的验收结果另行记录。固定上游源码、四份补丁与模型快照可独立构建引擎；本地 Actions 文件已编写，尚未执行远端 CI。
- `0.1.0-dev.7` 增加显式引擎升级迁移与准确制品绑定：在隔离副本核对产品已绑定会话，发布带证据的新配套检查点；宿主启动、模型重配置与配置恢复共用版本/hash 检查。迁移没有一键界面；暂存保留、未绑定/子会话和附件范围等限制见 [升级说明](engine-upgrade.md)。GitHub workflow 增加旧引擎资产前置条件，尚未配置资产或执行 hosted runner。
- `0.1.0-dev.8` 增加对话提取候选、逐条审阅、主体与转述归属、时间有效性、同范围替代、过期停止投影及 schema 3 迁移。后台专用会话拒绝工具、核对完整历史并清理；前台抢占、封存、来源删除、中断恢复和清理重试独立处理。自动提取默认关闭，启用后限额运行。详见 [对话记忆](conversation-memory.md)，具体发行验收另行绑定制品。
- 经验候选、独立调用计数、有限证据预测、人工启用、证据撤回重审。
- 浏览器工作台；回环绑定、随机令牌、Origin 与 Host 校验、CSP。
- 文件整理清单、目标冲突检测、原件保全、撤销与断点恢复。
- NTFS 活动库、U 盘完整代次、引擎配套数据库、单写者锁、分叉检测。
- 领域库与引擎数据库的配套恢复日志；先验证两者，再保全旧数据、安装和接续中断的恢复。
- Windows 稳定工作路径、独立编译入口、发行哈希校验、验收门禁、历史选择日志。

`.9` 本地实现持久草稿、修订冲突与明确合并、缺失目录只读恢复，以及同机可信保存日志的中断恢复和知识索引核对。草稿随 schema 4 身份检查点备份，文件恢复副本及宿主日志不随检查点跨机。具体边界见 [编辑恢复](workspace-recovery.md)，发行验收结果另行记录。

`.10` 本地实现只读子智能体协作面板：从任务根会话验证最多 32 个、4 层后代，显示状态、来源消息和去敏工具摘要；子会话内容不自动进入主聊天、记忆、经历或技能证据。固定 engine.6-source 的真实父子会话接口测试已通过，完整编译制品与浏览器验收结果另行绑定。具体边界见 [协作视图](collaboration.md)。

`.14` 在 schema 5 中增加产品委派账本，通过公共 OpenCode HTTP 和实际 `subagent` 白名单创建、暂停、继续及核对同一个子会话。创建或提示响应丢失时分别按所有权 metadata 和消息基线对账，不自动重发；未知子会话保持只读。显式交回会重新核验一条已完成来源，再由根会话汇总并进入普通验收，子正文不自动成为记忆或技能证据。固定 engine.6-source 的真实 agent、所有权 metadata、父子树和空历史已验证；完整发行结果另行绑定。

`.15` 修正定制引擎版本兼容性。固定上游提交本身是 `1.18.31`，旧的自定义 `0.0.0-product-*` 标识会被 OpenCode 免费服务拒绝；新构建使用真实上游版本，并继续依靠 `product-dev` 通道、禁用自动更新和 SHA-256 绑定保持专属发行身份。隔离免费模型请求已通过，完整产品与迁移结论以发行报告为准。

## 完整设计中的待推进项

- 对话记忆已实现审阅流程；仍需完成真实模型的中文归属、否定、条件和时间评估，再扩展人物/关系/世界认识。24 项人工预期夹具不代表实测准确率。
- 文件编辑的目录权限持久化、跨机文件恢复材料与项目上下文；全库图谱查询与大图性能、委派队列与多结果编排、独立 Infinite Canvas 视觉创作模块。当前图谱目录树仍仅覆盖已导入且本次已加载的资料，独立文件树按明确选择的根目录浏览。
- 候选整理与学习收益的 20–30 个真实任务对照集，情绪表达和恢复参数的使用体验校准。
- 模型技能提案、小范围评估、有界长期气质成长；当前仍采用固定气质和人工候选。
- 自动记忆压缩、归档与历史备份清理策略；目前显式删除不删除已有检查点、原生 OpenCode 会话或原始文件。
- 恢复分支与回滚的图形化选择、跨机版本迁移向导，以及删除旧备份的明确保留策略。
- 用户原生 OpenCode 会话的隐私删除/重建流程；目前独立记忆删除不修改原生会话。
- 原始资料、OpenCode 附件和项目依赖的全量可选迁移与离线可用性清单。
- PDF/OCR、语义索引、语音、视觉、Linux 启动模块、便携凭据加密。
- 大库与慢 U 盘性能测量、实际拔盘/断电故障试验、容量限额与缓存回收自动执行。
- Docker 沙盒按需执行集成；当前运行方式为 Windows 本机工作台与受权限约束的 OpenCode。

## 验收依据

测试分别覆盖领域不变量、真实 SQLite/WAL/锁、实际文件系统、实际浏览器、真实 OpenCode HTTP+工具执行，以及编译后的 Windows 主程序。真实模型服务在验收中由本地确定性 HTTP 夹具替代，不使用公网密钥；这验证协议与执行链路，不证明真实模型生成质量。

最终计数、制品哈希、版本及安装位置写入对应发行的 `release-validation.json` 与 `docs/verification.md`。任何跳过的真实后端测试不得计为发行通过。
